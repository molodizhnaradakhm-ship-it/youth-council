# Logs
logs
*.log
npm-debug.log*

# pnpm content-addressable store (belongs in ~/Library/pnpm/store, not the repo)
.pnpm-store

# Dependency directory
**/node_modules/**
_node_modules
.pnp.cjs

# Coverage directory used by tools like istanbul
coverage

.tmp
out/

_docpress
.DS_Store

dist
tsconfig.tsbuildinfo

# JetBrains IDEs
.idea/

/key

# pnpm uses npm for publishing a new version with
# dependencies bundled but the npm lockfile is not needed
# because pnpm use pnpm for installation
package-lock.json

__package_previews__
.store

privatePackages/store

## Verdaccio storage
storage

yarn.lock

RELEASE.md

.jest-cache
.verdaccio-cache
.turbo

# Deploy secrets (examples stay tracked)
.deploy/env.*.prod
!.deploy/env.*.prod.example
.deploy/env.*.dev
!.deploy/env.*.dev.example
.deploy/env.*.stg
!.deploy/env.*.stg.example
.deploy/media
